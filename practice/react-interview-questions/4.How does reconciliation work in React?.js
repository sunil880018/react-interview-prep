// Answer: Reconciliation is the process React uses to diff virtual DOM trees and apply changes efficiently. React uses:

// Keys to match elements.

// Tree diffing: same component types are compared recursively.

// Component diffing: different types = unmount + mount.
